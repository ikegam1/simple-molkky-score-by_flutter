# Simple Molkky Score

フィンランド発祥のスポーツ「モルック（Mölkky）」のスコアを管理するためのFlutterアプリです。
シンプルで直感的な操作感と、公式ルールに基づいた詳細なスコア管理、さらにクラウド同期（Firestore）を実現します。

## ✨ 主な機能
- **スコアボード機能:** 各ターンの得点と累計スコアを一覧表示。
- **ドラッグ＆ドロップ:** プレイヤーの投擲順を自由に並び替え可能。
- **クラウド同期 (NEW):** 試合終了時に Firestore へ対戦履歴を自動保存。
- **詳細履歴表示:** マッチの途中でも全セットの記録を確認可能。
- **公式ルール準拠:** 50点ピッタリ上がり、バースト、3ミス失格（サバイバル対応）など。

## 🚀 クラウド同期とデプロイの設定（重要）

本プロジェクトではセキュリティのため、Firebase の設定ファイル（`lib/firebase_options.dart`）をリポジトリに含めていません。
GitHub Actions を使って自動デプロイを行うためには、以下の手順で GitHub Secrets の設定が必要です。

### GitHub Secrets の設定手順

1. **Firebase 設定ファイルを Base64 エンコードする**
   お使いの Mac のターミナルで以下のコマンドを実行し、出力をコピーしてください。
   ```bash
   base64 -i lib/firebase_options.dart
   ```

2. **GitHub リポジトリの設定を開く**
   ブラウザで GitHub リポジトリを表示し、 **Settings > Secrets and variables > Actions** へ移動します。

3. **新しい Secret を追加する**
   [New repository secret] ボタンを押し、以下の内容で作成します：
   - **Name:** `FIREBASE_OPTIONS_BASE64`
   - **Value:** （手順 1 でコピーした文字列）

これだけで、次回からの `main` ブランチへのプッシュ時に、自動的に設定ファイルが復元され、ビルドとデプロイが成功するようになります。

## 🚀 ローカルでの起動方法

### 前提条件
- [Flutter SDK](https://docs.flutter.dev/get-started/install) がインストールされていること。

### 手順
1. リポジトリをクローンします。
2. 依存関係を解決します。
   ```bash
   flutter pub get
   ```
3. Webブラウザで起動します。
   ```bash
   flutter run -d chrome --web-port 5000
   ```

## 🛠️ 技術スタック
- Framework: Flutter (Web/Mobile)
- Database: Firebase Firestore
- Hosting: GitHub Pages
- CI/CD: GitHub Actions

---
Developed by M1 with ikegami.
